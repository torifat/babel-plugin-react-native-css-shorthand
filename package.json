{
  "name": "babel-plugin-react-native-css-shorthand",
  "version": "1.0.0",
  "description": "A babel plugin for React Native CSS Shorthand Syntax",
  "main": "lib/",
  "scripts": {
    "build": "babel src --out-dir lib",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/torifat/babel-plugin-react-native-css-shorthand.git"
  },
  "keywords": [
    "babel-plugin",
    "react-native"
  ],
  "author": "Rifat Nabi <to.rifat@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0"
  }
}
